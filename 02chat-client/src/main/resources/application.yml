spring:
  ai:
    dashscope:
      api-key: ${DASH_SCOPE_API_KEY}
      chat:
        options:
          model: qwen3.7-max

logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG
